{
  "name": "portfolio",
  "version": "0.1.0",
  "description": "Personal Portfolio application built with Meteor.",
  "scripts": {
    "start": "meteor --settings settings-development.json",
    "staging": "meteor deploy andytranstaging.meteor.com --settings settings-development.json",
    "production": "meteor deploy andy.design --settings settings.json"
  }
}